<!doctype html>
<html lang="cs">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Ověření</title>
  <style>
    html,body{height:100%;margin:0;font-family:Arial,Helvetica,sans-serif;
      background:#f4f6f8;display:flex;align-items:center;justify-content:center}
    .fake-button{
      background:#0b63d6;color:white;padding:16px 26px;border-radius:12px;
      border:none;font-size:18px;box-shadow:0 6px 18px rgba(11,99,214,0.25);
      cursor:pointer;
    }
    .fake-button:active{transform:translateY(1px)}
    #overlay {
      position:fixed; inset:0; display:none; align-items:center; justify-content:center;
      background:black; z-index:9999;
    }
    #overlay video { width:100%; height:100%; object-fit:cover; }
  </style>
</head>
<body>
  <!-- Tlačítko, které kamarád uvidí -->
  <button id="trigger" class="fake-button">Pokračovat</button>

  <!-- Overlay s videem -->
  <div id="overlay">
    <video id="prankVideo" playsinline preload="auto">
      <source src="prank.mp4" type="video/mp4">
    </video>
  </div>

  <script>
    const btn = document.getElementById('trigger');
    const overlay = document.getElementById('overlay');
    const vid = document.getElementById('prankVideo');

    btn.addEventListener('click', async () => {
      btn.style.display = 'none';        // schová tlačítko
      overlay.style.display = 'flex';    // ukáže overlay
      try {
        vid.currentTime = 0;
        vid.muted = false;               // zapne zvuk
        await vid.play();                // pustí video
      } catch(e){ console.error(e); }
    });
  </script>
</body>
</html>
